# CAD Files

Put the full robot CAD export here.

Suggested file names:

- `robot-arm-rover.step`
- `robot-arm-rover.stp`

STEP files can be large, so if GitHub rejects the upload later, use a release attachment or Git LFS.
